#points
  = @entry.points
  total points

%div
  - @categories.each do |category|
    = render :partial => 'categories/category', :locals => {:category => category, :entry => @entry}

- show_category_nav
