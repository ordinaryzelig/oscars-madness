- editable ||= false
.pick
  - if editable
    = radio_button_tag input_tag_name, nominee.id, pick.nominee_id == nominee.id
  - else
    = pick.nominee_id == nominee.id ? '<span>&rarr;</span>' : '&nbsp;'*4
