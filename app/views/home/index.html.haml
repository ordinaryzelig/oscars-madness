#standings
  %table
    %caption
      Standings
    %thead
      %tr#header.glossy_black
        %th
          Player
        %th
          Correct
        %th
          Points
    %tbody
      - @entries.each do |entry|
        = render :partial => 'players/standing', :locals => {:entry => entry}

= render @categories, :show_declare_winners => logged_in_as_admin?

- show_category_nav
