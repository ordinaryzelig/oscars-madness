.ui.grid.vertically.very.relaxed

  .row
    .column
      = render 'layouts/missing_picks'

  - if entries.any?
    .row
      .column
        = render 'standings'

  .row
    .column
      .categories
        = render categories
